name: Terraform CI/CD

on:
  push:
    branches:
      - main-lab02

jobs:
  deploy:
    name: Deploy Infrastructure and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Terraform Init
        run: cd infra | terraform init

      - name: Terraform Plan
        id: plan
        run: cd infra | terraform plan -out=tfplan

      - name: Check if plan is empty
        run: cd infra |
          echo "${{ steps.plan.outputs.stdout }}" | grep "No changes" && exit 78 || exit 0

      - name: Terraform Apply
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: cd infra | terraform apply tfplan -auto-approve

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: site/
          push: true
          tags: vitargo/14th-dalai-lama-site:latest